# 把這份複製一份改名為 .env(本機測試用),或直接把值貼到 Vercel 後台的 Environment Variables。
# 不要把填好值的 .env 上傳到 GitHub。
# 這兩個值來自 Supabase 後台 -> Project Settings -> API

# Project URL
VITE_SUPABASE_URL=

# anon / public key —— 在新版 Supabase 後台(API Keys 頁面)叫做「Publishable key」(sb_publishable_... 開頭)。
# 這不是密碼,設計上就是可以放在前端程式碼裡,真正的保護靠資料庫的 Row Level Security。
# 千萬不要填「Secret key」(sb_secret_... 開頭,舊稱 service_role key)——那把權限最高,
# 一旦填進這裡會被打包進瀏覽器看得到的程式碼,等於公開最高權限金鑰,是嚴重的資安風險。
VITE_SUPABASE_ANON_KEY=
